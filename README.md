<div align="center">
<img src="https://ik.imagekit.io/oiocs8j87/WhatsApp%20Image%202026-08-19%20at%2016.47.52.jpeg" width="200">


  


                                   

## ✨𝐎𝐥𝐚́, 𝐞𝐮 𝐬𝐨𝐮 𝐚 𝐓𝐡𝐚𝐢𝐬 !  

👩🏾‍💻 Desenvolvedora Java Full Stack em formação pela Generation Brasil.<br>
🎓 Bacharel em Administração de Empresas.<br>
📚 Estudando inglês e construindo minha carreira na tecnologia.<br>
🌎 Sonho em viajar o mundo, ajudar minha família e impactar vidas através da inovação.
</p>


&nbsp;




###  𝑻𝒆𝒄𝒏𝒐𝒍𝒐𝒈𝒊𝒂𝒔/𝑭𝒆𝒓𝒓𝒂𝒎𝒆𝒏𝒕𝒂𝒔
---

&nbsp;


<p align="center">
  <img src="https://skillicons.dev/icons?i=java,spring,mysql,git,github,html,css,vscode,eclipse" />
</p>

&nbsp;






 <p align="center">
  <img
    src="<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<title>Contribuições ❤</title>
<style>
  :root{
    --rosa: #ff6fb1;
    --rosa-forte: #ff2e93;
    --lilas: #b98af7;
    --lilas-forte: #7c4dff;
    --bg: #120818;
    --card: #1c0f26;
  }

  * { box-sizing: border-box; }

  html, body {
    margin: 0;
    height: 100%;
    background: radial-gradient(ellipse at top, #240f30 0%, var(--bg) 60%);
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .wrap {
    position: relative;
    padding: 40px 44px 34px;
    border-radius: 24px;
    background: linear-gradient(160deg, rgba(255,255,255,0.05), rgba(255,255,255,0.01));
    border: 1px solid rgba(255,255,255,0.08);
    box-shadow: 0 0 60px rgba(255,90,170,0.15), inset 0 0 40px rgba(120,60,200,0.08);
    backdrop-filter: blur(6px);
  }

  .title {
    text-align: center;
    color: #fbe9f7;
    font-size: 15px;
    letter-spacing: 3px;
    text-transform: uppercase;
    font-weight: 600;
    margin-bottom: 20px;
    opacity: 0.85;
  }

  .title span {
    background: linear-gradient(90deg, var(--rosa), var(--lilas), var(--rosa));
    background-size: 200% auto;
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    animation: shine 4s linear infinite;
  }

  @keyframes shine {
    to { background-position: 200% center; }
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(15, 15px);
    grid-template-rows: repeat(13, 15px);
    gap: 4px;
    position: relative;
    z-index: 2;
  }

  .cell {
    width: 15px;
    height: 15px;
    border-radius: 3px;
    background: rgba(255,255,255,0.04);
  }

  .cell.on {
    background: linear-gradient(135deg, var(--rosa), var(--lilas-forte));
    background-size: 300% 300%;
    animation: pulse-color 5s ease-in-out infinite, pop 0.5s ease backwards;
    box-shadow: 0 0 8px rgba(255,110,180,0.55), 0 0 2px rgba(255,255,255,0.4) inset;
  }

  @keyframes pulse-color {
    0%   { background-position: 0% 50%; filter: brightness(1); }
    25%  { background-position: 50% 100%; filter: brightness(1.25); }
    50%  { background-position: 100% 50%; filter: brightness(1); }
    75%  { background-position: 50% 0%; filter: brightness(1.35); }
    100% { background-position: 0% 50%; filter: brightness(1); }
  }

  @keyframes pop {
    from { transform: scale(0); opacity: 0; }
    to   { transform: scale(1); opacity: 1; }
  }

  .sparkle {
    position: absolute;
    pointer-events: none;
    color: #fff;
    opacity: 0;
    animation: twinkle 2.4s ease-in-out infinite;
    text-shadow: 0 0 6px var(--rosa), 0 0 10px var(--lilas);
  }

  @keyframes twinkle {
    0%, 100% { opacity: 0; transform: scale(0.3) rotate(0deg); }
    50%      { opacity: 1; transform: scale(1) rotate(90deg); }
  }

  .legend {
    margin-top: 22px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    color: rgba(255,255,255,0.55);
    font-size: 11px;
    letter-spacing: 1px;
  }

  .legend .sq {
    width: 11px; height: 11px; border-radius: 2px;
  }
  .sq.l1 { background: rgba(255,255,255,0.04); }
  .sq.l2 { background: linear-gradient(135deg, var(--rosa), var(--lilas-forte)); opacity:0.5;}
  .sq.l3 { background: linear-gradient(135deg, var(--rosa), var(--lilas-forte)); opacity:0.8;}
  .sq.l4 { background: linear-gradient(135deg, var(--rosa-forte), var(--lilas-forte)); }

  .float-heart {
    position: absolute;
    font-size: 14px;
    opacity: 0;
    animation: floatUp 6s ease-in infinite;
    filter: drop-shadow(0 0 6px var(--rosa));
  }

  @keyframes floatUp {
    0%   { opacity: 0; transform: translateY(0) scale(0.6); }
    10%  { opacity: 0.9; }
    80%  { opacity: 0.4; }
    100% { opacity: 0; transform: translateY(-160px) scale(1.1); }
  }
</style>
</head>
<body>

<div class="wrap" id="wrap">
  <div class="title"><span>minhas contribuições ✨ feitas com amor</span></div>
  <div class="grid" id="grid"></div>
  <div class="legend">
    <span>menos</span>
    <div class="sq l1"></div>
    <div class="sq l2"></div>
    <div class="sq l3"></div>
    <div class="sq l4"></div>
    <span>mais</span>
  </div>
</div>

<script>
  // Mapa de um coração pixelado em uma grade 15 (colunas) x 13 (linhas)
  const heartPattern = [
    "000110000110000",
    "001111011110100",
    "011111111111110",
    "011111111111110",
    "111111111111111",
    "111111111111111",
    "111111111111111",
    "011111111111110",
    "011111111111110",
    "001111111111100",
    "000111111111000",
    "000011111110000",
    "000001111100000",
  ];

  const grid = document.getElementById('grid');

  heartPattern.forEach((row, rIdx) => {
    [...row].forEach((val, cIdx) => {
      const cell = document.createElement('div');
      cell.classList.add('cell');
      if (val === '1') {
        cell.classList.add('on');
        // delays aleatórios pra ficar orgânico
        cell.style.animationDelay = `${Math.random() * 3}s, ${Math.random() * 0.6}s`;
      }
      grid.appendChild(cell);
    });
  });

  // Brilhos (sparkles) espalhados
  const wrap = document.getElementById('wrap');
  const sparkleChars = ['✦', '✧', '⋆', '✨'];
  for (let i = 0; i < 22; i++) {
    const s = document.createElement('div');
    s.classList.add('sparkle');
    s.textContent = sparkleChars[Math.floor(Math.random() * sparkleChars.length)];
    s.style.left = Math.random() * 100 + '%';
    s.style.top = Math.random() * 100 + '%';
    s.style.fontSize = (8 + Math.random() * 10) + 'px';
    s.style.animationDelay = (Math.random() * 3) + 's';
    s.style.animationDuration = (1.8 + Math.random() * 2) + 's';
    wrap.appendChild(s);
  }

  // Coraçõezinhos flutuando
  const hearts = ['💗','💜','💖'];
  for (let i = 0; i < 8; i++) {
    const h = document.createElement('div');
    h.classList.add('float-heart');
    h.textContent = hearts[Math.floor(Math.random() * hearts.length)];
    h.style.left = (10 + Math.random() * 80) + '%';
    h.style.bottom = '0px';
    h.style.animationDelay = (Math.random() * 6) + 's';
    h.style.animationDuration = (5 + Math.random() * 3) + 's';
    wrap.appendChild(h);
  }
</script>

</body>
</html>"
    alt="GitHub Jet Heatmap"
    width="85%"
  />
</p>
</a>


</div>


